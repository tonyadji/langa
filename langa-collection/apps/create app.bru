meta {
  name: create app
  type: http
  seq: 4
}

post {
  url: {{langa-api}}/api/applications
  body: json
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

body:json {
  {
    "name":"langa2"
  }
}
